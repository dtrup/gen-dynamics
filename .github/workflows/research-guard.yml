name: Research guard

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run unit tests
        run: python -m unittest discover -s tests -v
      - name: Validate greenhouse
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            python scripts/research_guard.py validate --base "origin/${{ github.base_ref }}"
          else
            python scripts/research_guard.py validate
          fi

  guarded-auto-merge:
    if: >-
      github.event_name == 'pull_request' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(github.event.pull_request.labels.*.name, 'safe-auto-merge')
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Squash-merge validated safe change
        env:
          GH_TOKEN: ${{ github.token }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        # Private repositories on GitHub Free cannot enable branch protection or
        # native auto-merge. The label gate plus the completed validate job is the
        # repository's merge guard, so merge directly only after both have passed.
        run: gh pr merge "$PR_URL" --squash --delete-branch
